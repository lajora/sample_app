<% @micropost.comments.each do |comment|%>
    <div class="comment-content">
        <p><%= link_to comment.user.name, comment.user%> commented <%= time_ago_in_words(comment.created_at) %> ago:</p>
        <%= comment.content%>
    </div>
<% end %>
